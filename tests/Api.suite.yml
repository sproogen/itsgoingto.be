class_name: ApiTester
modules:
    enabled:
        - REST:
            url: /api
            depends: Symfony
        - Symfony:
            app_path: 'src'
            var_path: '../var'
            environment: 'test'
            rebootable_client: false
        - Doctrine2:
            depends: Symfony
            cleanup: true
        - Helper\Api

