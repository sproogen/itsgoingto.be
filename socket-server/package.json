{
  "name": "itsgoingtobe-socket-serer",
  "version": "0.0.1",
  "scripts": {
    "start": "nodemon index.js --exec babel-node",
    "build": "babel index.js -d dist",
    "serve": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "express": "4.16.4",
    "socket.io": "2.1.1",
    "socket.io-client": "^2.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.18.5"
  }
}
